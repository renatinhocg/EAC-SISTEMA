{
  "name": "eac-pwa-full-stack",
  "version": "1.0.0",
  "description": "Full stack EAC PWA project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd ../pwa && npm install && npm run build",
    "build:admin": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "build:pwa": "cd pwa && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../pwa && npm install",
    "popular-banco": "node popular_banco.js"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "lucide-react": "^0.534.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.14.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  }
}
