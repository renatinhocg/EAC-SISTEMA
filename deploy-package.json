{
  "name": "deploy-backend",
  "version": "1.0.0",
  "description": "Deploy script for backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "npm install --production"
  }
}
